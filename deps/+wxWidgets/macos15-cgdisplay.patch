diff --git a/src/osx/carbon/dcscreen.cpp b/src/osx/carbon/dcscreen.cpp
index 1c6a987f96..7f5920da00 100644
--- a/src/osx/carbon/dcscreen.cpp
+++ b/src/osx/carbon/dcscreen.cpp
@@ -65,11 +65,12 @@ wxBitmap wxScreenDCImpl::DoGetAsBitmap(const wxRect *subrect) const
     CGContextTranslateCTM( context, 0,  m_height );
     CGContextScaleCTM( context, 1, -1 );
 
+    CGImageRef image = NULL;
+
+#if __MAC_OS_X_VERSION_MAX_ALLOWED < 150000
     if ( subrect )
         srcRect = CGRectOffset( srcRect, -subrect->x, -subrect->y ) ;
 
-    CGImageRef image = NULL;
-    
     image = CGDisplayCreateImage(kCGDirectMainDisplay);
 
     wxASSERT_MSG(image, wxT("wxScreenDC::GetAsBitmap - unable to get screenshot."));
@@ -77,6 +78,10 @@ wxBitmap wxScreenDCImpl::DoGetAsBitmap(const wxRect *subrect) const
     CGContextDrawImage(context, srcRect, image);
 
     CGImageRelease(image);
+#else
+    (void)image;
+    (void)srcRect;
+#endif
 
     CGContextRestoreGState(context);
 #else
@@ -84,3 +89,4 @@ wxBitmap wxScreenDCImpl::DoGetAsBitmap(const wxRect *subrect) const
 #endif
     return bmp;
 }
+
